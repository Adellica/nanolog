;; -*- scheme -*-

;; TODO: make a nlog talk to server via daemon so we don't halt everything

(compile nlog.scm)
(install-program 'nlog '("nlog"))

(compile nlogd.scm)
(install-program 'nlogd '("nlogd"))

;; TODO: compile libnlog so
;; TODO: compile nlog program
;; TODO: compile nlogd program
